# Changelog

## [Unreleased]

### Changed
- Updated Windows PowerShell commands to use explicit boolean values (`[bool]1` and `[bool]0`) instead of `$true` and `$false` for consistency and clarity. This affects the addition, modification, and removal of IP aliases on Windows systems.