# Changelog

## [Unreleased]

### Changed
- Updated the OpenSSH configuration for Windows to set correct permissions on the `administrators_authorized_keys` file, ensuring that the Administrators group has full access to the file. The `authorized_keys` file is now copied to the correct location with the required permissions.