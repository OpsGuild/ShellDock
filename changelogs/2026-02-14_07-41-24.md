# Changelog

## [Unreleased]

### Changed
- Updated Windows command to add IP address with netmask and gateway.
- Modified Windows persistent IP alias configuration to include gateway.

### Added
- Added optional `gateway` parameter to IP alias configuration for all platforms, allowing users to specify a default gateway when adding an alias IP address.