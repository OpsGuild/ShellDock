# Changelog

## [Unreleased]

### Changed
- Improved macOS system log cleanup to handle cases where sudo is unavailable
- Enhanced Docker prune behavior on macOS with configurable timeout and optional enable flag via `SHELLDOCK_ENABLE_DARWIN_DOCKER_PRUNE` environment variable

### Fixed
- Test sandbox now properly cleans up temporary directories on exit
- Test execution timeout detection now correctly identifies actual timeout messages