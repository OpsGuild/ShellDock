# Changelog

## [Unreleased]

### Changed
- Improved the command to show current IP configuration for better readability and accuracy.
- Modified the command to verify alias IP was added to show the IP configuration for a specific interface.
- Updated the command to restart the network adapter after adding an alias IP address.
- Changed the command to remove alias IP address from interface to improve accuracy.
- Updated the command to test network connectivity in the `reset-network.yaml` file for Windows.
- Modified the commands to generate SSH key pair, set permissions, and display public key in the `openssh.yaml` file for Windows.

### Added
- Added a new command to restart the network adapter after adding an alias IP address.

### Fixed
- No explicit bug fixes were found in the provided diff, but the changes made may have resolved some underlying issues.