# Changelog

## [Unreleased]

### Changed
- Improved SSH connection instructions for Windows users, now displaying the hostname and IPv4 address for easier connection.
- Updated the display of SSH key pair generation, now showing the locations of the private and public keys, and providing commands to view and add the keys to the SSH agent.
- Simplified the SSH key pair generation output, removing the automatic display of the public key and instead providing instructions on how to view it.