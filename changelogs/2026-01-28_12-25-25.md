# Changelog

## [Unreleased]

### Changed
- Improved error handling in file and directory processing by ensuring that resources are properly closed, even in the event of an error.
- Modified the `getCommandForPlatform` and `getCommandForPlatformShow` functions to simplify the check for the presence of platform commands.
- Updated the `syncCmd` command to ignore errors when closing files and removing temporary files.
- Enhanced the `processDirectory` and `downloadFile` functions to check for HTTP status codes and handle errors more robustly, ensuring that resources are properly closed in all scenarios.