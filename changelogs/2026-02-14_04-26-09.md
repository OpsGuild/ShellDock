# Changelog

## [Unreleased]

### Changed
- Updated the command for setting permissions on `administrators_authorized_keys` on Windows to use the `copy` command instead of `Copy-Item` and corrected the syntax for granting permissions with `icacls`.