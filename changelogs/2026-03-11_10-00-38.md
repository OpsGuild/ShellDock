# Changelog

## [Unreleased]

### Changed
- Updated installation URLs in README and documentation to point directly to GitHub raw content for Debian/Ubuntu, CentOS/RHEL, and Arch Linux install scripts
- Improved command reliability by replacing `echo -e` with `printf` for Redis memory configuration, alias IP setup, and hostname updates
- Enhanced unattended upgrades configuration by using heredoc syntax for better readability and maintainability
- Improved Caddyfile creation using heredoc syntax for better formatting and reliability

### Fixed
- Fixed potential issues with special character interpretation in shell commands by replacing `echo -e` with more robust alternatives
- Improved consistency in configuration file generation across different platforms