# Changelog

## [Unreleased]

### Changed
- Improved formatting and quoting in various shell commands across the repository.
- Updated `.github/workflows/test.yml` to remove unnecessary whitespace and improve branch specification.
- Enhanced `.pre-commit-config.yaml` with additional hooks for YAML validation and linting.
- Modified `repository/network/reset-network.yaml` to improve Windows command quoting and formatting.
- Updated `repository/security/openssh.yaml` with improved quoting and formatting for Windows commands.
- Changed `repository/test.yaml` and other files to remove trailing whitespace and improve formatting.
- Improved quoting and formatting in `test/test-multi-version.yaml` for better consistency and readability. 

### Removed
- Removed unnecessary blank lines from various files across the repository for better formatting and readability.